package club.asyncraft.asynchat;

object BuildConstants {
    const val PLUGIN_ID = "${plugin_id}";
    const val PLUGIN_NAME = "${plugin_name}";
    const val PLUGIN_VERSION = "${plugin_version}";
    const val PLUGIN_DESCRIPTION = "${plugin_description}";
    const val PLUGIN_URL = "${plugin_url}";
    const val PLUGIN_AUTHORS = "${plugin_authors}";
}